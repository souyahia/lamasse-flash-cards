import { Text, View } from "react-native";

export const SuspenseFallback = () => {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text>TODO : Add Suspense</Text>
    </View>
  );
};
