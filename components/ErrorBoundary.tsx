import { Text, View } from "react-native";

export const ErrorBoundary = () => {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text>TODO : Add Error Boundary</Text>
    </View>
  );
};
